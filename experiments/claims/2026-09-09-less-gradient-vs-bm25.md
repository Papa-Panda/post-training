## [2026-09-09] Claim: gradient alignment 选出的数据优于 BM25（示例）

> 这是一条示例，展示模板怎么填。第一条真实 claim 请按此格式新建文件。

- 来源：LESS 论文（ai-data 笔记 Day04）
- 知识图谱节点：目标化选择
- 可证伪表述：如果在 SST-2 子集上用 100 样本 warmup 模型计算梯度、
  选出 500 样本做 finetune，那么其准确率显著高于随机选 500 样本；
  **算我错的标准是：两者差距 < 1% 或为负。**
- 我的预测（跑之前写）：我赌 gradient 赢 2-3 个点，因为 BM25 只看文本相似，
  而梯度对齐捕捉的是"对当前模型有用"的方向；但如果 warmup 太弱导致梯度噪声大，
  差距可能缩小到 1% 以内。
- 实验设计：Tier 1；SST-2 子集；warmup 模型 = TinyBERT 100 样本 1 epoch；
  指标 = test accuracy，跑 3 个 seed 取均值。
- 结果：（待跑）
- 解读 + 下一步：（待填）
